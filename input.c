/*
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
*/
#include <stdio.h>
/*
*/

struct point {
    int x, y, z;
};

union foo {
    int i;
    long l;
    const char *s;
    struct point p;
};

union bar;
/*
union bar { int i;};
*/

int main()
{
    /*
    union bar b;
    union foo f;
    */
    union foo f;
    union foo f;
    int i;
    int i;
    int i;

here:
here:
here:

    return 12;
}
